# ИНСТРУКЦИЯ ДЛЯ QWEN - ЖЕСТКИЕ ПРАВИЛА
# AI DEVELOPER GUIDELINES - ЖЕСТКИЕ ТЕХНИЧЕСКИЕ ПРАВИЛА

## 🚨 АБСОЛЮТНЫЕ ЗАПРЕТЫ
1. **НИКАКИХ ОТКЛОНЕНИЙ ОТ TECH_STACK_FINAL.md** - использовать ТОЛЬКО указанные версии
2. **НИКАКИХ НЕОБРАТИМЫХ ИЗМЕНЕНИЙ** - все миграции ДОЛЖНЫ иметь rollback
3. **НИКАКИХ КОМПРОМИССОВ С БЕЗОПАСНОСТЬЮ** - RLS, хеширование, валидация ОБЯЗАТЕЛЬНЫ
4. **НИКАКИХ ИЗМЕНЕНИЙ БЕЗ ТЕСТОВ** - coverage >=80% для нового кода

## 🎯 ПРОЦЕСС РАЗРАБОТКИ (СТРОГО ПОЭТАПНО)

### ЭТАП 1: DDL МИГРАЦИИ
-- ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ:
-- 1. RLS на ВСЕХ таблицах с tenant_id
-- 2. Индексы на ВСЕХ foreign keys
-- 3. CHECK constraints для ВСЕХ enum полей
-- 4. Комментарии к таблицам и колонкам
-- 5. Обратные операции для КАЖДОЙ миграции

ЭТАП 2: SQLAlchemy МОДЕЛИ
# ТРЕБОВАНИЯ:
# - Type hints для ВСЕХ полей
# - Relationship с каскадными операциями
# - RLS интеграция через tenant_id
# - Валидация на уровне моделей

ЭТАП 3: Pydantic СХЕМЫ
# ТРЕБОВАНИЯ:
# - Strict validation с кастомными валидаторами
# - Сериализация/десериализация JSON
# - OpenAPI документация автоматически
# - Обработка ошибок с детализацией

ЭТАП 4: FastAPI ENDPOINTS
# ТРЕБОВАНИЯ:
# - JWT аутентификация на КАЖДОМ endpoint
# - Rate limiting по умолчанию
# - OpenAPI документация автоматически
# - Обработка ВСЕХ возможных ошибок

ЭТАП 5: ТЕСТИРОВАНИЕ
# МИНИМАЛЬНЫЕ ТРЕБОВАНИЯ:
# - Unit tests для ВСЕХ моделей и сервисов
# - Integration tests для ВСЕХ API endpoints
# - Тесты RLS изоляции между tenantами
# - Тесты производительности для критичных путей

🔧 ТЕХНИЧЕСКИЕ СТАНДАРТЫ (БЕЗ ИСКЛЮЧЕНИЙ)
PYTHON СТАНДАРТЫ
# ОБЯЗАТЕЛЬНО:
# - Python 3.11.7+ (точно указанная версия)
# - Type hints для ВСЕХ функций и методов
# - Black + flake8 + mypy = 0 ошибок
# - Pydantic v2+ с strict mode
# - Async/await для ВСЕХ I/O операций

БАЗА ДАННЫХ
-- ОБЯЗАТЕЛЬНО:
-- - PostgreSQL 15+ с RLS на ВСЕХ таблицах
-- - SQLAlchemy 2.0+ с async поддержкой
-- - Alembic для ВСЕХ миграций
-- - Индексы на ВСЕХ foreign keys
-- - Constraints для ВСЕХ бизнес-правил

API СТАНДАРТЫ
# ОБЯЗАТЕЛЬНО:
# - FastAPI 0.110.0+ с автоматической OpenAPI
# - JWT аутентификация с refresh tokens
# - Rate limiting: 1000 req/hour по умолчанию
# - Валидация ВСЕХ входных параметров
# - Стандартизированные error responses

БЕЗОПАСНОСТЬ
# ОБЯЗАТЕЛЬНО:
# - Multi-tenancy через RLS на ВСЕХ уровнях
# - Хеширование паролей bcrypt
# - Валидация ВСЕХ пользовательских входных данных
# - Защита от SQL injection, XSS, CSRF
# - Audit logging для ВСЕХ критичных операций

📋 КРИТЕРИИ ПРИЕМКИ КАЖДОГО МОДУЛЯ
ДЛЯ DDL МИГРАЦИЙ:
Все таблицы имеют RLS политики
Все foreign keys имеют индексы
Все enum поля имеют CHECK constraints
Миграции reversible (есть rollback скрипты)
Комментарии к таблицам и колонкам

ДЛЯ API ENDPOINTS:
JWT аутентификация на всех endpoints
Rate limiting настроен
OpenAPI документация автоматически генерируется
Валидация всех входных параметров
Обработка всех возможных ошибок

ДЛЯ ТЕСТОВ:
Coverage >=80% для нового кода
Unit tests для всех моделей и сервисов
Integration tests для всех API endpoints
Тесты RLS изоляции между tenantами
Тесты производительности для критичных путей

❌ ЧТО НЕ ДЕЛАТЬ (АБСОЛЮТНЫЕ ЗАПРЕТЫ)
НЕ ИСПОЛЬЗОВАТЬ технологии не из TECH_STACK_FINAL.md
НЕ ПИСАТЬ код без type hints
НЕ СОЗДАВАТЬ API endpoints без аутентификации
НЕ ДЕЛАТЬ миграции без rollback скриптов
НЕ ИГНОРИРОВАТЬ multi-tenancy на любом уровне
НЕ ПРОПУСКАТЬ тесты для любого функционала
НЕ ОТКЛОНЯТЬСЯ от ARCHITECTURE.md структуры
НЕ ДОБАВЛЯТЬ функционал вне scope текущего модуля

🎯 ПРОЦЕСС РЕВЬЮ КОДА
Каждый сгенерированный файл ДОЛЖЕН пройти проверку:
Архитектурное соответствие - точное следование ARCHITECTURE.md
Технологическое соответствие - строго по TECH_STACK_FINAL.md
Качество кода - type hints, error handling, documentation
Безопасность - RLS, validation, authentication
Тестирование - coverage, edge cases, performance

⚡ ЭСКАЛАЦИЯ ПРОБЛЕМ
При любых сомнениях или неясностях:
ОСТАНОВИТЬ разработку
ЗАФИКСИРОВАТЬ вопрос
ОБРАТИТЬСЯ к техническому директору за решением
ПРОДОЛЖИТЬ только после получения четких инструкций

ЭТИ ПРАВИЛА НЕ ПОДЛЕЖАТ ИНТЕРПРЕТАЦИИ ИЛИ ИЗМЕНЕНИЮ БЕЗ СОГЛАСОВАНИЯ С ТЕХНИЧЕСКИМ ДИРЕКТОРОМ
